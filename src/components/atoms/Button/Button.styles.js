import styled from 'styled-components';

export const StyledButton = styled.button`
  width: 2rem;
  aspect-ratio: 1;
  border-radius: 50%;
  background-color: #ccc;
  border: none;
  display: grid;
  place-items: center;
  cursor: pointer;
`;
